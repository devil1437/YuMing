cmake_minimum_required(VERSION 2.8)
PROJECT(YUMING)

SET(YUMING_SRC
	RSD-DVS.c
)

SET(MY_C_FLAGS "-Wall -lm")
SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${MY_C_FLAGS}")

INCLUDE_DIRECTORIES(${YUMING_SOURCE_DIR}/include)
ADD_EXECUTABLE(multi ${YUMING_SRC})
